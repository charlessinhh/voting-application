pipeline {
    agent any
    stages {
        stage("Docker version"){
            steps{
                sh "Docker --version"
                sh "pwd"
                sh "ls"
            }
        }
    }
}
